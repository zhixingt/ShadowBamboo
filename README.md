# ShadowBamboo

## Show Current Branch

Use `show-current-branch.sh` to print the name of the current git branch:

```bash
bash show-current-branch.sh
```

This script relies on `git branch --show-current`, which is available in Git 2.22 and later.